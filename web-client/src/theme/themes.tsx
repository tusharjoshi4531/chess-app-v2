import { createTheme } from "@mui/material";

export const mainTheme = createTheme({
    palette: {
        primary: {
            main: "#263238",
        },
        secondary: {
            main: "#ff4081",
        },
        error: {
            main: "#d50000",
        },
    },
});
